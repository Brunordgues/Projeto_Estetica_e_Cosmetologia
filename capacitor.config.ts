import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solicitacoes_sistema.app',
  appName: 'solicitacoes_sistema',
  webDir: 'dist'
};

export default config;
